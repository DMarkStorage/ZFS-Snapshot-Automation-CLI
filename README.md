# ZFS-Snapshot-Automation-CLI
Python CLI tool for automating ZFS snapshot management via REST API. Create, delete, list, and verify snapshots across filesystems with built-in CSV/JSON reporting. Designed for XCP migration workflows, backup scripting, and monitoring—removing the need for manual console operations.
